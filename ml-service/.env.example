# ML Service environment variables
# Copy to .env and fill in — never commit the real .env

# Port mappings (host:container)
BEHAVIORAL_PORT=8080
FORENSIC_PORT=8000

# Log level: debug | info | warning | error
LOG_LEVEL=info
